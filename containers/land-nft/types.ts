export type Position = "top" | "right" | "bottom" | "left";

export type ImageSize = {
  width: number;
  height: number;
};

export type BreakpointSizes = {
  lg: ImageSize;
  md: ImageSize;
  sm: ImageSize;
  xs: ImageSize;
};
